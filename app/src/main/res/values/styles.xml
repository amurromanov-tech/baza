<?xml version="1.0" encoding="utf-8"?>
<resources>

      <!-- ============================================================ -->
    <!-- СТИЛИ КНОПОК ДЕЙСТВИЙ -->
    <!-- ============================================================ -->

    <!-- Кнопка: Редактировать (зелёная) -->
    <style name="ActionButton.Edit" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">#4CAF50</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Кнопка: Выдать (жёлтая) -->
    <style name="ActionButton.Lend" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">#FFC107</item>
        <item name="android:textColor">#212121</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Кнопка: Архивировать (жёлтая) -->
    <style name="ActionButton.Archive" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">#FFC107</item>
        <item name="android:textColor">#212121</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Кнопка: Перенести (синяя) -->
    <style name="ActionButton.Move" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">#2196F3</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Кнопка: Копировать (синяя) -->
    <style name="ActionButton.Copy" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">#2196F3</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Кнопка: Удалить (красная) -->
    <style name="ActionButton.Delete" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">#F44336</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- ============================================================ -->
    <!-- СТИЛИ ДЛЯ ПЛАВАЮЩИХ КНОПОК (FAB) — ЕСЛИ НУЖНО -->
    <!-- ============================================================ -->
    <style name="FabButton" parent="Widget.MaterialComponents.ExtendedFloatingActionButton">
        <item name="cornerRadius">16dp</item>
        <item name="android:textSize">14sp</item>
    </style>

</resources>
