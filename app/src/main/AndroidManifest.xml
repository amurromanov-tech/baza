<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===== РАЗРЕШЕНИЯ ===== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- ============================================================ -->
    <!-- QUERIES (для Android 11+)                                     -->
    <!-- Нужно, чтобы intent.resolveActivity() и запуск установщика   -->
    <!-- APK работали на Android 11+ (package visibility)             -->
    <!-- ============================================================ -->
    <queries>
        <!-- Установщик APK -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/vnd.android.package-archive" />
        </intent>
        <!-- Браузер (для OAuth и внешних ссылок) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BAZA"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true">

        <!-- ===== FILE PROVIDER ===== -->
        <!-- Используется для:                                        -->
        <!-- 1. Передачи файла APK установщику (обновления)           -->
        <!-- 2. Передачи фото камере (съёмка предметов)               -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- ============================================================ -->
        <!-- ГЛАВНЫЕ ACTIVITIES -->
        <!-- ============================================================ -->

        <!-- Вход (launcher) -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Главный экран -->
        <activity android:name=".ui.MainActivity" />

        <!-- Сканер штрих-кодов (только портрет) -->
        <activity
            android:name=".ui.BarcodeScannerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- ============================================================ -->
        <!-- ЭКРАНЫ ДОБАВЛЕНИЯ И РЕДАКТИРОВАНИЯ -->
        <!-- ============================================================ -->

        <!-- Добавление предмета -->
        <activity android:name=".ui.AddItemActivity" />

        <!-- Детали предмета -->
        <activity android:name=".ui.ItemDetailActivity" />

        <!-- Подключение к семье -->
        <activity android:name=".ui.ConnectFamilyActivity" />

        <!-- Полноэкранный просмотр фото (pinch-to-zoom) -->
        <activity
            android:name=".ui.FullscreenImageActivity"
            android:exported="false"
            android:theme="@style/Theme.BAZA.Fullscreen" />

        <!-- ============================================================ -->
        <!-- НАСТРОЙКИ И РАЗДЕЛЫ -->
        <!-- ============================================================ -->

        <!-- Настройки (главный экран) -->
        <activity android:name=".ui.SettingsActivity" />

        <!-- Статистика и учёт -->
        <activity android:name=".ui.StatsAndAccountingActivity" />

        <!-- Резервное копирование -->
        <activity android:name=".ui.BackupActivity" />

        <!-- Диагностика -->
        <activity android:name=".ui.DiagnosticsActivity" />

        <!-- Настройки приложения (тема, язык и т.д.) -->
        <activity android:name=".ui.AppSettingsActivity" />

        <!-- ============================================================ -->
        <!-- СТАТИСТИКА, АРХИВ, ЗАЙМЫ -->
        <!-- ============================================================ -->

        <!-- Статистика расходов -->
        <activity android:name=".ui.StatisticsActivity" />

        <!-- Архив предметов -->
        <activity android:name=".ui.ArchiveActivity" />

        <!-- Выданные предметы -->
        <activity android:name=".ui.LentItemsActivity" />

        <!-- ============================================================ -->
        <!-- OAUTH REDIRECT -->
        <!-- ============================================================ -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com.family.base"
                    android:host="oauth2redirect" />
            </intent-filter>
        </activity>

    </application>
</manifest>
